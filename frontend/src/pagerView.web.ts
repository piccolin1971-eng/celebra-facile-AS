// Stub web di react-native-pager-view: il modulo è native-only, quindi su
// web esportiamo null. celebra.tsx riconosce il caso e usa un fallback
// (rendering pagina-per-pagina con bottoni prev/next).
const PagerView: any = null;
export default PagerView;
