/**
 * Risoluzione TypeScript per `import PagerView from "../src/pagerView"`.
 * Metro bundler usa in runtime pagerView.native.ts (Android/iOS) o pagerView.web.ts (web).
 */
export { default } from "./pagerView.native";
